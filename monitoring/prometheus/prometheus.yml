global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'routing-simulator-backend'
    metrics_path: '/api/metrics/prometheus'
    static_configs:
      - targets: ['backend:8000']
        labels:
          service: 'routing-simulator'
          component: 'backend'

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
